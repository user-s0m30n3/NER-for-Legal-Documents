# =============================================================
# VeriSum-Legal — Requirements
# Python 3.11.x | GPU: RTX 3050 (CUDA, Ampere SM 8.6)
# =============================================================

# ---- Core Inference ----
transformers>=4.40.0
torch>=2.1.0
pdfplumber>=0.10.0
sentencepiece>=0.1.99
accelerate>=0.28.0

# ---- NER: OpenNyAI spaCy pipeline ----
spacy>=3.7.0
spacy-transformers>=1.3.0
# en_legal_ner_trf and en_core_web_sm are installed separately via pip wheel URLs
# See README.md for install commands

# ---- NLP Utilities ----
nltk>=3.8.0          # Used for Levenshtein distance in precedent clustering
wasabi>=0.10.0       # EkStep soft dependency (warn messages)

# ---- Summarization ----
# facebook/bart-base loaded via transformers — no extra package needed

# ---- Quantization (GPU only — RTX 3050) ----
bitsandbytes>=0.43.0

# ---- Evaluation ----
rouge-score>=0.1.2

# ---- Compatibility Pin — seqeval breaks with numpy>=2.0 ----
numpy<2.0

# ---- Training scripts (offline use only) ----
datasets>=2.18.0
seqeval>=1.2.2

# ---- UI (Phase 2 — not needed yet) ----
streamlit>=1.32.0
